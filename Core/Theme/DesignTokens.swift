//
//  DesignTokens.swift
//
//  Backward compatibility shim.
//  All design system values now live in DesignSystem.swift.
//  This file exists only so existing code referencing `DesignTokens` still compiles.
//
//  `DesignTokens` is a typealias for `DS` (defined in DesignSystem.swift).
//  You can safely delete this file once all references use `DS` directly.
//

// The typealias `DesignTokens = DS` is declared at the bottom of DesignSystem.swift.
// Nothing else is needed here.

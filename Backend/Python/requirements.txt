# TheSwiftKit AI Backend — Python dependencies
# Install with: pip install -r requirements.txt

flask>=3.0.0
flask-cors>=5.0.0
python-dotenv>=1.0.1

# Provider SDKs — you only need the one matching LLM_PROVIDER, but both are
# listed so switching providers is friction-free.
openai>=1.68.0
google-generativeai>=0.8.0
